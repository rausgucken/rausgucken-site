{
  "name": "rausgucken-site",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && npx pagefind --site dist --output-path dist/pagefind",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^6.0.0",
    "pagefind": "^1.3.0"
  }
}
